<nav class="nav-shell">
    <div class="nav-group">
        <div class="nav-brand">Parrot</div>
            <div class="nav-links">
    {% if app.user %}
        {% if is_granted('ROLE_ADMIN') %}
            <a class="nav-link" href="{{ path('app_post_index') }}">Gazouillis</a>
            <a class="nav-link" href="{{ path('app_reply_index') }}">Reponses</a>
            <a class="nav-link" href="{{ path('app_tag_index') }}">Tags</a>
        {% endif %}
        <a class="nav-link" href="{{ path('app_user_index') }}">Utilisateurs</a>
        <a class="nav-link" href="{{ path('app_post_timeline') }}">Fil d'actualite</a>
        <a class="nav-link" href="{{ path('app_discussion_index') }}">Discussions</a>
        <a class="nav-link" href="{{ path('app_notification_index') }}">
            Notifications{% if app.user.countNotification > 0 %} ({{ app.user.countNotification }}){% endif %}
        </a>
        <a class="nav-link" href="{{ path('app_user_profile_edit') }}">{{ app.user.userName }}</a>
        <a class="nav-link" href="{{ path('app_logout') }}">Se deconnecter</a>
    {% endif %}
        </div>
    </div>
</nav>
