<form id="suppr{{contact.id}}" method="post" action="{{ path('app_post_delete', {'id': contact.id}) }}" class="w-full">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact.id) }}">    
    <button type="button" data-modal-toggle="popup-modal" data-modal-target="popup-modal" class="btn-soft-violet suppr">Supprimer</button>
</form>