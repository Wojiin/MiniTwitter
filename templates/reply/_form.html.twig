{{ form_start(form, {'attr': {'class': 'form-card form-stack'}}) }}
    {{ form_widget(form) }}
    <div class="form-actions">
        <button class="btn-primary">{{ button_label|default('Sauvegarder') }}</button>
    </div>
{{ form_end(form) }}
