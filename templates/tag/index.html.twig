{% extends 'base.html.twig' %}

{% block title %}Liste des Tags{% endblock %}

{% block body %}
    <div class="page-stack-wide">
        <h1 class="section-title-lg">Tags</h1>

        <div class="table-shell">
            <table class="table">
                <thead>
                    <tr>
                        <th>Categorie</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for tag in tags %}
                    <tr>
                        <td>{{ tag.category }}</td>
                        <td class="table-actions">
                            <a class="table-link" href="{{ path('app_tag_show', {'id': tag.id}) }}">Detail</a>
                            <a class="table-link" href="{{ path('app_tag_edit', {'id': tag.id}) }}">Modifier</a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="2" class="table-empty">Aucun tag présent</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>

        <a class="btn-primary" href="{{ path('app_tag_new') }}">Créer un nouveau tag</a>
    </div>
{% endblock %}
