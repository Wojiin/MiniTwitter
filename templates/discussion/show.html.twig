{% extends 'base.html.twig' %}

{% block title %}Discussion{% endblock %}

{% block body %}
    <h1>Discussion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ discussion.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ discussion.title }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ discussion.createdAt ? discussion.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ discussion.updatedAt ? discussion.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_discussion_index') }}">back to list</a>

    <a href="{{ path('app_discussion_edit', {'id': discussion.id}) }}">edit</a>

    {{ include('discussion/_delete_form.html.twig') }}
{% endblock %}
